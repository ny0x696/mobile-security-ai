# Case Manager

Initialize and maintain one case per target. Preserve `target.json`, `scope.json`, `inventory.json`, `endpoints.json`, `hypotheses.json`, `evidence.json`, `findings.json`, plus static/native/runtime/network/scripts/artifacts/report directories.

Every artifact gets a stable ID and provenance. Keep state transitions explicit and resumable.
